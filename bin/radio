#!/usr/bin/env ruby
require 'audio_addict'

runner = AudioAddict::CLI.runner

begin
  exit runner.run ARGV
rescue AudioAddict::Interrupt
  say! "\nGoodbye"
  exit 1  
rescue => e
  puts e.backtrace.reverse if ENV['DEBUG']
  say! "!undred!#{e.class}!txtrst!\n#{e.message}"
  exit 1
end